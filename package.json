{
  "private": true,
  "scripts": {
    "dotenvx": "dotenvx",
    "build": "dotenvx run -- turbo build",
    "dev": "dotenvx run -- turbo dev",
    "type-check": "turbo type-check",
    "clean": "turbo clean",
    "lint": "dotenvx run -- biome check .",
    "lint:fix": "dotenvx run -- biome check . --write"
  },
  "devDependencies": {
    "@biomejs/biome": "1.8.3",
    "@playwright/test": "^1.45.1",
    "@types/node": "^20.11.24",
    "prettier": "^3.2.5",
    "prettier-plugin-tailwindcss": "^0.5.11",
    "turbo": "^2.0.6"
  },
  "packageManager": "pnpm@8.15.6",
  "engines": {
    "node": ">=18"
  },
  "name": "with-tailwind",
  "dependencies": {
    "@dotenvx/dotenvx": "^1.6.2"
  }
}
